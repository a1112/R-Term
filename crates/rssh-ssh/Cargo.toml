[package]
name = "rterm-ssh-adapter"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[lib]
name = "rssh_ssh"

[features]
runtime-adapter = []

[dependencies]
ssh-core = { package = "rssh-ssh", git = "file:///H:/project/R-SSH-split-trial/R-SSH", rev = "5bcbedbc652d1a92844e5bac5ed9a48a013c12a0" }
rterm-runtime = { path = "../rssh-runtime", version = "0.1.0" }
rterm-types = { path = "../rterm-types", version = "0.1.0" }

[dev-dependencies]
rssh-domain = { path = "../rssh-domain", version = "0.1.0" }

[lints]
workspace = true
